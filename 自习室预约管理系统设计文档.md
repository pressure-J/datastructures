# 自习室预约管理系统设计文档（C语言版）

## 一、问题分析与理解
### 1 问题描述
### 1.1 业务场景
某校图书馆向全体学生免费开放自习阅览室，由于疫情管控需要，要求：
- 学生每日预约，凭预约记录（扫码）进出
- 严格进行1人1桌1座管理
- 空闲时间超过一定时长即预约失效
- 提高座位使用效率

### 1.2 核心需求
1. **预约管理**：学生可预约未来时间的座位
2. **签到管理**：预约后需在规定时间内扫码签到
3. **座位管理**：实时监控座位使用状态
4. **超时失效**：签到后空闲超过阈值自动释放座位
5. **数据统计**：使用率、预约率等统计

### 1.3 约束条件
- 同一时间段，一个座位只能被一个学生预约
- 同一学生同一时间只能预约一个座位
- 预约需在每日开放时间内
- 签到后空闲超过30分钟自动取消预约

---

## 二、数据结构设计（C语言）

### 2.1 枚举类型定义

```c
/* 座位状态枚举 */
typedef enum {
    SEAT_AVAILABLE,      /* 可用 */
    SEAT_RESERVED,       /* 已预约 */
    SEAT_OCCUPIED,       /* 使用中 */
    SEAT_MAINTENANCE,    /* 维护中 */
    SEAT_DISABLED        /* 禁用 */
} SeatStatus;

/* 预约状态枚举 */
typedef enum {
    RES_PENDING,         /* 待签到 */
    RES_CHECKED_IN,      /* 已签到（使用中） */
    RES_COMPLETED,       /* 已完成 */
    RES_CANCELLED,       /* 已取消 */
    RES_EXPIRED,         /* 已过期（未签到） */
    RES_TIMEOUT          /* 超时失效（空闲超时） */
} ReservationStatus;

/* 错误码枚举 */
typedef enum {
    SUCCESS = 0,
    ERR_STUDENT_NOT_FOUND,      /* 学生不存在 */
    ERR_SEAT_NOT_FOUND,         /* 座位不存在 */
    ERR_SEAT_UNAVAILABLE,       /* 座位不可用 */
    ERR_TIME_CONFLICT,          /* 时间冲突 */
    ERR_RESERVATION_NOT_FOUND,  /* 预约不存在 */
    ERR_INVALID_TIME,           /* 无效时间 */
    ERR_CREDIT_INSUFFICIENT,    /* 信用分不足 */
    ERR_CHECK_IN_TIMEOUT,       /* 签到超时 */
    ERR_MEMORY_ALLOC,           /* 内存分配失败 */
    ERR_SYSTEM_ERROR            /* 系统错误 */
} ErrorCode;
```

### 2.2 核心数据结构

#### 2.2.1 学生信息（Student）
```c
#define MAX_STUDENT_ID_LEN  20
#define MAX_NAME_LEN        50
#define MAX_PHONE_LEN       15
#define MAX_DEPT_LEN        100

typedef struct Student {
    char studentId[MAX_STUDENT_ID_LEN];  /* 学号（唯一标识） */
    char name[MAX_NAME_LEN];             /* 姓名 */
    char phone[MAX_PHONE_LEN];           /* 联系电话 */
    char department[MAX_DEPT_LEN];       /* 院系 */
    int creditScore;                     /* 信用分（初始100） */
    time_t registerTime;                 /* 注册时间 */
    struct Student *next;                /* 链表指针 */
} Student;
```

#### 2.2.2 自习室信息（StudyRoom）
```c
#define MAX_ROOM_ID_LEN    20
#define MAX_ROOM_NAME_LEN  50
#define MAX_LOCATION_LEN   100
#define MAX_TIME_LEN       10

typedef struct StudyRoom {
    char roomId[MAX_ROOM_ID_LEN];        /* 自习室编号 */
    char roomName[MAX_ROOM_NAME_LEN];    /* 自习室名称 */
    int capacity;                        /* 座位容量 */
    char location[MAX_LOCATION_LEN];     /* 位置 */
    char openTime[MAX_TIME_LEN];         /* 开放时间（如 "08:00"） */
    char closeTime[MAX_TIME_LEN];        /* 关闭时间（如 "22:00"） */
    int isActive;                        /* 是否启用（1启用，0禁用） */
    struct StudyRoom *next;              /* 链表指针 */
} StudyRoom;
```

#### 2.2.3 座位信息（Seat）
```c
#define MAX_SEAT_ID_LEN  30

typedef struct Seat {
    char seatId[MAX_SEAT_ID_LEN];        /* 座位编号（如 "A101-001"） */
    char roomId[MAX_ROOM_ID_LEN];        /* 所属自习室 */
    int row;                             /* 排 */
    int column;                          /* 列 */
    SeatStatus status;                   /* 座位状态 */
    char currentStudent[MAX_STUDENT_ID_LEN]; /* 当前使用学生学号 */
    time_t lastActiveTime;               /* 最后活跃时间 */
    struct Seat *next;                   /* 链表指针 */
} Seat;
```

#### 2.2.4 预约记录（Reservation）
```c
#define MAX_RES_ID_LEN  30
#define MAX_REASON_LEN  200

typedef struct Reservation {
    char reservationId[MAX_RES_ID_LEN];  /* 预约编号（唯一标识） */
    char studentId[MAX_STUDENT_ID_LEN];  /* 学生学号 */
    char seatId[MAX_SEAT_ID_LEN];        /* 座位编号 */
    char roomId[MAX_ROOM_ID_LEN];        /* 自习室编号 */
    time_t reserveTime;                  /* 预约时间 */
    time_t startTime;                    /* 预约开始时间 */
    time_t endTime;                      /* 预约结束时间 */
    time_t checkInTime;                  /* 实际签到时间 */
    time_t checkOutTime;                 /* 实际签退时间 */
    ReservationStatus status;            /* 预约状态 */
    char cancelReason[MAX_REASON_LEN];   /* 取消原因 */
    struct Reservation *next;            /* 链表指针 */
} Reservation;
```

#### 2.2.5 时间段信息（TimeSlot）
```c
typedef struct TimeSlot {
    char slotId[MAX_RES_ID_LEN];         /* 时间段编号 */
    char roomId[MAX_ROOM_ID_LEN];        /* 自习室编号 */
    time_t startTime;                    /* 开始时间 */
    time_t endTime;                      /* 结束时间 */
    int availableSeats;                  /* 可用座位数 */
    struct TimeSlot *next;               /* 链表指针 */
} TimeSlot;
```

### 2.3 哈希表结构设计

#### 2.3.1 哈希表节点
```c
#define HASH_TABLE_SIZE  101  /* 哈希表大小（素数） */

/* 学生哈希表节点 */
typedef struct StudentHashNode {
    char key[MAX_STUDENT_ID_LEN];  /* 学号作为键 */
    Student *value;                /* 学生信息 */
    struct StudentHashNode *next;  /* 链表处理冲突 */
} StudentHashNode;

/* 座位哈希表节点 */
typedef struct SeatHashNode {
    char key[MAX_SEAT_ID_LEN];    /* 座位号作为键 */
    Seat *value;                  /* 座位信息 */
    struct SeatHashNode *next;    /* 链表处理冲突 */
} SeatHashNode;

/* 预约哈希表节点 */
typedef struct ReservationHashNode {
    char key[MAX_RES_ID_LEN];    /* 预约号作为键 */
    Reservation *value;          /* 预约信息 */
    struct ReservationHashNode *next; /* 链表处理冲突 */
} ReservationHashNode;
```

#### 2.3.2 哈希表结构
```c
/* 学生哈希表 */
typedef struct {
    StudentHashNode *buckets[HASH_TABLE_SIZE];
    int count;                   /* 元素数量 */
} StudentHashTable;

/* 座位哈希表 */
typedef struct {
    SeatHashNode *buckets[HASH_TABLE_SIZE];
    int count;
} SeatHashTable;

/* 预约哈希表 */
typedef struct {
    ReservationHashNode *buckets[HASH_TABLE_SIZE];
    int count;
} ReservationHashTable;
```

### 2.4 链表结构设计

#### 2.4.1 学生预约记录链表（按学生ID索引）
```c
/* 学生预约记录节点 */
typedef struct StudentReservationNode {
    char studentId[MAX_STUDENT_ID_LEN];
    Reservation *reservations;  /* 该学生的所有预约（链表） */
    int count;                  /* 预约数量 */
    struct StudentReservationNode *next;
} StudentReservationNode;

/* 学生预约索引表 */
typedef struct {
    StudentReservationNode *head;
    int count;
} StudentReservationIndex;
```

#### 2.4.2 座位预约记录链表（按座位ID索引）
```c
/* 座位预约记录节点 */
typedef struct SeatReservationNode {
    char seatId[MAX_SEAT_ID_LEN];
    Reservation *reservations;  /* 该座位的所有预约（链表） */
    int count;
    struct SeatReservationNode *next;
} SeatReservationNode;

/* 座位预约索引表 */
typedef struct {
    SeatReservationNode *head;
    int count;
} SeatReservationIndex;
```

### 2.5 优先队列（用于超时检测）

```c
/* 超时事件节点 */
typedef struct TimeoutEvent {
    time_t timeout;                     /* 超时时间 */
    char reservationId[MAX_RES_ID_LEN]; /* 预约编号 */
} TimeoutEvent;

/* 最小堆（优先队列） */
typedef struct {
    TimeoutEvent *events;    /* 事件数组 */
    int capacity;            /* 容量 */
    int size;                /* 当前大小 */
} MinHeap;
```

### 2.6 栈结构（用于撤销操作）

```c
/* 操作记录节点 */
typedef struct OperationNode {
    char operation[50];       /* 操作类型 */
    char reservationId[MAX_RES_ID_LEN];
    ReservationStatus oldStatus;
    ReservationStatus newStatus;
    struct OperationNode *next;
} OperationNode;

/* 操作栈 */
typedef struct {
    OperationNode *top;
    int count;
} OperationStack;
```

### 2.7 队列结构（用于等待队列）

```c
/* 等待队列节点 */
typedef struct WaitQueueNode {
    char studentId[MAX_STUDENT_ID_LEN];
    char seatId[MAX_SEAT_ID_LEN];
    time_t requestTime;
    struct WaitQueueNode *next;
} WaitQueueNode;

/* 等待队列 */
typedef struct {
    WaitQueueNode *front;
    WaitQueueNode *rear;
    int count;
} WaitQueue;
```

---

## 三、系统功能设计

### 3.1 功能模块划分

```
自习室预约管理系统（C语言版）
├── 基础数据结构模块
│   ├── 链表操作（创建、插入、删除、查找、销毁）
│   ├── 哈希表操作（初始化、插入、查找、删除、扩容）
│   ├── 栈操作（初始化、入栈、出栈、判空）
│   ├── 队列操作（初始化、入队、出队、判空）
│   └── 最小堆操作（初始化、插入、删除堆顶、调整）
├── 学生管理模块
│   ├── 学生注册
│   ├── 学生登录验证
│   ├── 获取学生信息
│   ├── 更新学生信息
│   ├── 扣除信用分
│   └── 查询信用分
├── 座位管理模块
│   ├── 查询可用座位
│   ├── 获取座位信息
│   ├── 更新座位状态
│   ├── 推荐座位
│   └── 获取座位布局
├── 预约管理模块
│   ├── 创建预约
│   ├── 取消预约
│   ├── 签到
│   ├── 签退
│   ├── 获取预约信息
│   ├── 查询学生预约记录
│   ├── 查询座位预约记录
│   ├── 检查时间段冲突
│   └── 自动处理过期预约
├── 超时管理模块
│   ├── 启动空闲计时器
│   ├── 停止空闲计时器
│   ├── 更新活跃时间
│   ├── 检查超时
│   ├── 处理超时
│   └── 批量检查超时
├── 统计分析模块
│   ├── 获取使用率
│   ├── 获取预约率
│   ├── 获取高峰时段
│   ├── 获取违约统计
│   ├── 获取座位热度排行
│   └── 生成统计报告
└── 系统管理模块
    ├── 添加自习室
    ├── 更新自习室信息
    ├── 删除自习室
    ├── 添加座位
    ├── 批量添加座位
    ├── 更新座位信息
    ├── 删除座位
    ├── 系统参数配置
    └── 获取系统参数
```

### 3.2 核心功能详细设计

#### 3.2.1 预约功能
**输入**：学生学号、自习室编号、座位编号、预约时间段
**处理流程**：
1. 验证学生身份和信用分
2. 检查座位是否可用
3. 检查时间段是否冲突
4. 检查学生是否有其他预约冲突
5. 创建预约记录
6. 更新座位状态为"已预约"
7. 返回预约成功信息

**时间复杂度**：O(n) - 链表遍历查找冲突

#### 3.2.2 签到功能
**输入**：预约编号、学生学号
**处理流程**：
1. 验证预约记录存在且状态为"待签到"
2. 验证当前时间在预约时间内
3. 更新预约状态为"已签到"
4. 更新座位状态为"使用中"
5. 记录签到时间
6. 启动空闲计时器

**时间复杂度**：O(1) - 哈希表查找

#### 3.2.3 签退功能
**输入**：预约编号
**处理流程**：
1. 验证预约记录存在且状态为"已签到"
2. 更新预约状态为"已完成"
3. 更新座位状态为"可用"
4. 记录签退时间
5. 停止空闲计时器

**时间复杂度**：O(1)

#### 3.2.4 超时检测功能
**处理逻辑**：
1. 定期检查所有"已签到"状态的预约
2. 计算当前时间与最后活跃时间的差值
3. 若差值超过阈值（30分钟），执行超时处理
4. 更新预约状态为"超时失效"
5. 释放座位
6. 扣除学生信用分

**时间复杂度**：O(n) - 遍历所有已签到预约

#### 3.2.5 座位查询功能
**输入**：自习室编号、时间范围
**输出**：该时间段内可用座位列表
**处理逻辑**：
1. 获取指定自习室的所有座位
2. 查询每个座位在指定时间段的预约情况
3. 筛选出无预约冲突的座位
4. 返回可用座位列表

**时间复杂度**：O(m * n) - m个座位，每个座位n次查找

---

## 四、函数封装设计

### 4.1 基础数据结构函数

#### 4.1.1 链表操作函数
```c
/* 创建新节点 */
Student* createStudentNode(const Student *data);
Seat* createSeatNode(const Seat *data);
Reservation* createReservationNode(const Reservation *data);

/* 链表插入（头插法） */
int insertStudent(Student **head, const Student *data);
int insertSeat(Seat **head, const Seat *data);
int insertReservation(Reservation **head, const Reservation *data);

/* 链表查找 */
Student* findStudent(Student *head, const char *studentId);
Seat* findSeat(Seat *head, const char *seatId);
Reservation* findReservation(Reservation *head, const char *reservationId);

/* 链表删除 */
int deleteStudent(Student **head, const char *studentId);
int deleteSeat(Seat **head, const char *seatId);
int deleteReservation(Reservation **head, const char *reservationId);

/* 销毁链表 */
void destroyStudentList(Student **head);
void destroySeatList(Seat **head);
void destroyReservationList(Reservation **head);

/* 获取链表长度 */
int getStudentListLength(Student *head);
int getSeatListLength(Seat *head);
int getReservationListLength(Reservation *head);
```

#### 4.1.2 哈希表操作函数
```c
/* 哈希函数 */
unsigned int hashString(const char *key);

/* 哈希表初始化 */
int initStudentHashTable(StudentHashTable *table);
int initSeatHashTable(SeatHashTable *table);
int initReservationHashTable(ReservationHashTable *table);

/* 哈希表插入 */
int insertStudentToHash(StudentHashTable *table, const Student *student);
int insertSeatToHash(SeatHashTable *table, const Seat *seat);
int insertReservationToHash(ReservationHashTable *table, const Reservation *res);

/* 哈希表查找 */
Student* findStudentInHash(StudentHashTable *table, const char *studentId);
Seat* findSeatInHash(SeatHashTable *table, const char *seatId);
Reservation* findReservationInHash(ReservationHashTable *table, const char *resId);

/* 哈希表删除 */
int deleteStudentFromHash(StudentHashTable *table, const char *studentId);
int deleteSeatFromHash(SeatHashTable *table, const char *seatId);
int deleteReservationFromHash(ReservationHashTable *table, const char *resId);

/* 销毁哈希表 */
void destroyStudentHashTable(StudentHashTable *table);
void destroySeatHashTable(SeatHashTable *table);
void destroyReservationHashTable(ReservationHashTable *table);
```

#### 4.1.3 栈操作函数
```c
/* 栈初始化 */
int initOperationStack(OperationStack *stack);

/* 入栈 */
int pushOperation(OperationStack *stack, const OperationNode *data);

/* 出栈 */
int popOperation(OperationStack *stack, OperationNode *data);

/* 判空 */
int isStackEmpty(OperationStack *stack);

/* 销毁栈 */
void destroyOperationStack(OperationStack *stack);
```

#### 4.1.4 队列操作函数
```c
/* 队列初始化 */
int initWaitQueue(WaitQueue *queue);

/* 入队 */
int enqueueWait(WaitQueue *queue, const WaitQueueNode *data);

/* 出队 */
int dequeueWait(WaitQueue *queue, WaitQueueNode *data);

/* 判空 */
int isQueueEmpty(WaitQueue *queue);

/* 销毁队列 */
void destroyWaitQueue(WaitQueue *queue);
```

#### 4.1.5 最小堆操作函数
```c
/* 最小堆初始化 */
int initMinHeap(MinHeap *heap, int capacity);

/* 插入 */
int insertToMinHeap(MinHeap *heap, const TimeoutEvent *event);

/* 删除堆顶 */
int extractMin(MinHeap *heap, TimeoutEvent *event);

/* 查看堆顶 */
int peekMin(MinHeap *heap, TimeoutEvent *event);

/* 调整堆 */
void heapifyUp(MinHeap *heap, int index);
void heapifyDown(MinHeap *heap, int index);

/* 销毁堆 */
void destroyMinHeap(MinHeap *heap);
```

### 4.2 学生管理函数

```c
/* 学生注册 */
ErrorCode registerStudent(StudentHashTable *table, const Student *student);

/* 学生登录验证 */
ErrorCode loginStudent(StudentHashTable *table, const char *studentId, 
                       const char *password, Student *result);

/* 获取学生信息 */
ErrorCode getStudentInfo(StudentHashTable *table, const char *studentId, 
                         Student *result);

/* 更新学生信息 */
ErrorCode updateStudentInfo(StudentHashTable *table, const char *studentId, 
                            const Student *newInfo);

/* 扣除信用分 */
ErrorCode deductCredit(StudentHashTable *table, const char *studentId, 
                       int points);

/* 查询信用分 */
ErrorCode getCreditScore(StudentHashTable *table, const char *studentId, 
                         int *score);
```

### 4.3 座位管理函数

```c
/* 查询可用座位 */
ErrorCode getAvailableSeats(SeatHashTable *seatTable, 
                            ReservationHashTable *resTable,
                            const char *roomId, 
                            time_t startTime, time_t endTime,
                            Seat **resultList, int *count);

/* 获取座位信息 */
ErrorCode getSeatInfo(SeatHashTable *table, const char *seatId, 
                      Seat *result);

/* 更新座位状态 */
ErrorCode updateSeatStatus(SeatHashTable *table, const char *seatId, 
                           SeatStatus newStatus);

/* 推荐座位 */
ErrorCode recommendSeats(SeatHashTable *seatTable, 
                         ReservationHashTable *resTable,
                         const char *studentId,
                         time_t startTime, time_t endTime,
                         Seat **resultList, int *count);

/* 获取座位布局 */
ErrorCode getSeatLayout(SeatHashTable *table, const char *roomId,
                        Seat ***layout, int *rows, int *cols);
```

### 4.4 预约管理函数

```c
/* 生成预约ID */
void generateReservationId(char *id, int size);

/* 创建预约 */
ErrorCode createReservation(StudentHashTable *studentTable,
                            SeatHashTable *seatTable,
                            ReservationHashTable *resTable,
                            const char *studentId,
                            const char *seatId,
                            time_t startTime, time_t endTime,
                            Reservation *result);

/* 取消预约 */
ErrorCode cancelReservation(ReservationHashTable *resTable,
                            SeatHashTable *seatTable,
                            const char *reservationId,
                            const char *reason);

/* 签到 */
ErrorCode checkIn(ReservationHashTable *resTable,
                  SeatHashTable *seatTable,
                  const char *reservationId,
                  const char *studentId);

/* 签退 */
ErrorCode checkOut(ReservationHashTable *resTable,
                   SeatHashTable *seatTable,
                   const char *reservationId);

/* 获取预约信息 */
ErrorCode getReservationInfo(ReservationHashTable *table,
                             const char *reservationId,
                             Reservation *result);

/* 查询学生预约记录 */
ErrorCode getStudentReservations(ReservationHashTable *table,
                                 const char *studentId,
                                 Reservation **resultList, int *count);

/* 查询座位预约记录 */
ErrorCode getSeatReservations(ReservationHashTable *table,
                              const char *seatId,
                              Reservation **resultList, int *count);

/* 检查时间段冲突 */
int hasTimeConflict(ReservationHashTable *table,
                    const char *seatId,
                    time_t startTime, time_t endTime);

/* 检查学生时间冲突 */
int hasStudentTimeConflict(ReservationHashTable *table,
                           const char *studentId,
                           time_t startTime, time_t endTime);

/* 自动处理过期预约 */
void processExpiredReservations(ReservationHashTable *resTable,
                                SeatHashTable *seatTable,
                                StudentHashTable *studentTable);
```

### 4.5 超时管理函数

```c
/* 超时管理器结构 */
typedef struct {
    MinHeap timeoutHeap;              /* 超时事件堆 */
    int timeoutThreshold;             /* 超时阈值（分钟） */
    ReservationHashTable *resTable;   /* 预约表引用 */
    SeatHashTable *seatTable;         /* 座位表引用 */
    StudentHashTable *studentTable;   /* 学生表引用 */
} TimeoutManager;

/* 初始化超时管理器 */
int initTimeoutManager(TimeoutManager *manager, int threshold);

/* 启动空闲计时器 */
void startIdleTimer(TimeoutManager *manager, const char *reservationId);

/* 停止空闲计时器 */
void stopIdleTimer(TimeoutManager *manager, const char *reservationId);

/* 更新活跃时间 */
void updateActiveTime(TimeoutManager *manager, const char *reservationId);

/* 检查超时 */
int checkTimeout(TimeoutManager *manager, const char *reservationId);

/* 处理超时 */
void handleTimeout(TimeoutManager *manager, const char *reservationId);

/* 批量检查超时 */
void batchCheckTimeout(TimeoutManager *manager);

/* 获取超时阈值 */
int getTimeoutThreshold(TimeoutManager *manager);

/* 设置超时阈值 */
void setTimeoutThreshold(TimeoutManager *manager, int minutes);

/* 销毁超时管理器 */
void destroyTimeoutManager(TimeoutManager *manager);
```

### 4.6 统计分析函数

```c
/* 统计报告结构 */
typedef struct {
    char roomId[MAX_ROOM_ID_LEN];
    time_t startTime;
    time_t endTime;
    double usageRate;          /* 使用率 */
    double reservationRate;    /* 预约率 */
    int totalReservations;     /* 总预约数 */
    int completedReservations; /* 完成预约数 */
    int cancelledReservations; /* 取消预约数 */
    int timeoutReservations;   /* 超时预约数 */
    int peakHour;              /* 高峰时段 */
} StatisticsReport;

/* 获取使用率 */
double getUsageRate(ReservationHashTable *resTable,
                    const char *roomId,
                    time_t startTime, time_t endTime);

/* 获取预约率 */
double getReservationRate(ReservationHashTable *resTable,
                          const char *roomId,
                          time_t startTime, time_t endTime);

/* 获取高峰时段 */
int getPeakHour(ReservationHashTable *resTable,
                const char *roomId,
                time_t startTime, time_t endTime);

/* 获取违约统计 */
ErrorCode getViolationStats(StudentHashTable *studentTable,
                            time_t startTime, time_t endTime,
                            int *violationCount, double *avgCredit);

/* 获取座位热度排行 */
ErrorCode getSeatPopularity(ReservationHashTable *resTable,
                            const char *roomId,
                            char seatIds[][MAX_SEAT_ID_LEN],
                            int *counts, int *numSeats);

/* 生成统计报告 */
ErrorCode generateReport(ReservationHashTable *resTable,
                         const char *roomId,
                         time_t startTime, time_t endTime,
                         StatisticsReport *report);
```

### 4.7 系统管理函数

```c
/* 系统配置结构 */
typedef struct {
    char key[50];
    char value[200];
} SystemConfig;

/* 系统管理器结构 */
typedef struct {
    StudentHashTable studentTable;      /* 学生哈希表 */
    SeatHashTable seatTable;            /* 座位哈希表 */
    ReservationHashTable resTable;      /* 预约哈希表 */
    StudyRoom *roomList;                /* 自习室链表 */
    TimeoutManager timeoutManager;      /* 超时管理器 */
    OperationStack undoStack;           /* 撤销栈 */
    SystemConfig configs[50];           /* 系统配置 */
    int configCount;                    /* 配置数量 */
} SystemManager;

/* 初始化系统 */
int initSystem(SystemManager *manager);

/* 销毁系统 */
void destroySystem(SystemManager *manager);

/* 添加自习室 */
ErrorCode addStudyRoom(SystemManager *manager, const StudyRoom *room);

/* 更新自习室信息 */
ErrorCode updateStudyRoom(SystemManager *manager, const char *roomId,
                          const StudyRoom *newInfo);

/* 删除自习室 */
ErrorCode deleteStudyRoom(SystemManager *manager, const char *roomId);

/* 添加座位 */
ErrorCode addSeat(SystemManager *manager, const Seat *seat);

/* 批量添加座位 */
ErrorCode batchAddSeats(SystemManager *manager, const char *roomId,
                        int rows, int cols);

/* 更新座位信息 */
ErrorCode updateSeat(SystemManager *manager, const char *seatId,
                     const Seat *newInfo);

/* 删除座位 */
ErrorCode deleteSeat(SystemManager *manager, const char *seatId);

/* 系统参数配置 */
ErrorCode setSystemConfig(SystemManager *manager, 
                          const char *key, const char *value);

/* 获取系统参数 */
ErrorCode getSystemConfig(SystemManager *manager,
                          const char *key, char *value, int valueSize);

/* 保存数据到文件 */
ErrorCode saveDataToFile(SystemManager *manager, const char *filename);

/* 从文件加载数据 */
ErrorCode loadDataFromFile(SystemManager *manager, const char *filename);
```

### 4.8 等待队列管理函数

```c
/* 加入等待队列 */
ErrorCode joinWaitQueue(WaitQueue *queue, const char *studentId,
                        const char *seatId);

/* 从等待队列取出 */
ErrorCode leaveWaitQueue(WaitQueue *queue, char *studentId,
                         char *seatId, int idSize);

/* 查看队首 */
ErrorCode peekWaitQueue(WaitQueue *queue, char *studentId,
                        char *seatId, int idSize);

/* 获取队列长度 */
int getWaitQueueLength(WaitQueue *queue);
```

---

## 五、关键算法设计（C语言实现）

### 5.1 哈希函数
```c
/* BKDR哈希函数 */
unsigned int hashString(const char *key) {
    unsigned int seed = 131;  /* 31 131 1313 13131 131313 etc.. */
    unsigned int hash = 0;
    
    while (*key) {
        hash = hash * seed + (*key++);
    }
    
    return (hash & 0x7FFFFFFF) % HASH_TABLE_SIZE;
}
```

### 5.2 冲突检测算法
```c
/* 检查时间区间冲突 */
int hasTimeConflict(ReservationHashTable *table,
                    const char *seatId,
                    time_t startTime, time_t endTime) {
    /* 遍历该座位的所有预约 */
    for (int i = 0; i < HASH_TABLE_SIZE; i++) {
        ReservationHashNode *node = table->buckets[i];
        while (node != NULL) {
            if (strcmp(node->value->seatId, seatId) == 0 &&
                node->value->status != RES_CANCELLED &&
                node->value->status != RES_COMPLETED) {
                /* 检查时间是否重叠 */
                if (startTime < node->value->endTime && 
                    endTime > node->value->startTime) {
                    return 1;  /* 有冲突 */
                }
            }
            node = node->next;
        }
    }
    return 0;  /* 无冲突 */
}
```

### 5.3 最小堆调整算法
```c
/* 上浮调整 */
void heapifyUp(MinHeap *heap, int index) {
    while (index > 0) {
        int parent = (index - 1) / 2;
        if (heap->events[index].timeout < heap->events[parent].timeout) {
            /* 交换 */
            TimeoutEvent temp = heap->events[index];
            heap->events[index] = heap->events[parent];
            heap->events[parent] = temp;
            index = parent;
        } else {
            break;
        }
    }
}

/* 下沉调整 */
void heapifyDown(MinHeap *heap, int index) {
    int smallest = index;
    int left = 2 * index + 1;
    int right = 2 * index + 2;
    
    if (left < heap->size && 
        heap->events[left].timeout < heap->events[smallest].timeout) {
        smallest = left;
    }
    
    if (right < heap->size && 
        heap->events[right].timeout < heap->events[smallest].timeout) {
        smallest = right;
    }
    
    if (smallest != index) {
        /* 交换 */
        TimeoutEvent temp = heap->events[index];
        heap->events[index] = heap->events[smallest];
        heap->events[smallest] = temp;
        heapifyDown(heap, smallest);
    }
}
```

### 5.4 链表排序算法（归并排序）
```c
/* 链表归并排序 */
Reservation* mergeSortReservation(Reservation *head) {
    if (head == NULL || head->next == NULL) {
        return head;
    }
    
    /* 找中点 */
    Reservation *slow = head;
    Reservation *fast = head->next;
    while (fast != NULL && fast->next != NULL) {
        slow = slow->next;
        fast = fast->next->next;
    }
    
    Reservation *mid = slow->next;
    slow->next = NULL;
    
    /* 递归排序 */
    Reservation *left = mergeSortReservation(head);
    Reservation *right = mergeSortReservation(mid);
    
    /* 合并 */
    return mergeReservationLists(left, right);
}

/* 合并两个有序链表 */
Reservation* mergeReservationLists(Reservation *a, Reservation *b) {
    if (a == NULL) return b;
    if (b == NULL) return a;
    
    Reservation *result = NULL;
    if (a->startTime <= b->startTime) {
        result = a;
        result->next = mergeReservationLists(a->next, b);
    } else {
        result = b;
        result->next = mergeReservationLists(a, b->next);
    }
    return result;
}
```

---

## 六、数据结构选择理由

| 数据结构 | 用途 | 选择理由 |
|---------|------|---------|
| 链表 | 动态存储 | C语言原生支持，动态大小，频繁插入删除 |
| 哈希表 | 快速查找 | O(1)平均时间复杂度，适合频繁查询 |
| 栈 | 撤销操作 | LIFO特性，支持操作回退 |
| 队列 | 等待队列 | FIFO特性，公平处理等待请求 |
| 最小堆 | 超时检测 | O(log n)插入删除，自动排序 |

---

## 七、内存管理策略

### 7.1 内存分配
```c
/* 安全的内存分配 */
void* safeMalloc(size_t size) {
    void *ptr = malloc(size);
    if (ptr == NULL) {
        fprintf(stderr, "内存分配失败\n");
        exit(EXIT_FAILURE);
    }
    memset(ptr, 0, size);
    return ptr;
}

/* 安全的内存重分配 */
void* safeRealloc(void *ptr, size_t size) {
    void *newPtr = realloc(ptr, size);
    if (newPtr == NULL) {
        fprintf(stderr, "内存重分配失败\n");
        exit(EXIT_FAILURE);
    }
    return newPtr;
}
```

### 7.2 内存释放
```c
/* 释放学生节点 */
void freeStudentNode(Student *node) {
    if (node != NULL) {
        free(node);
    }
}

/* 释放座位节点 */
void freeSeatNode(Seat *node) {
    if (node != NULL) {
        free(node);
    }
}

/* 释放预约节点 */
void freeReservationNode(Reservation *node) {
    if (node != NULL) {
        free(node);
    }
}
```

---

## 八、异常处理设计

### 8.1 错误处理函数
```c
/* 获取错误信息 */
const char* getErrorMessage(ErrorCode code) {
    switch (code) {
        case SUCCESS:
            return "操作成功";
        case ERR_STUDENT_NOT_FOUND:
            return "学生不存在";
        case ERR_SEAT_NOT_FOUND:
            return "座位不存在";
        case ERR_SEAT_UNAVAILABLE:
            return "座位不可用";
        case ERR_TIME_CONFLICT:
            return "时间冲突";
        case ERR_RESERVATION_NOT_FOUND:
            return "预约不存在";
        case ERR_INVALID_TIME:
            return "无效时间";
        case ERR_CREDIT_INSUFFICIENT:
            return "信用分不足";
        case ERR_CHECK_IN_TIMEOUT:
            return "签到超时";
        case ERR_MEMORY_ALLOC:
            return "内存分配失败";
        case ERR_SYSTEM_ERROR:
            return "系统错误";
        default:
            return "未知错误";
    }
}

/* 错误日志记录 */
void logError(ErrorCode code, const char *function, int line) {
    time_t now = time(NULL);
    char timeStr[20];
    strftime(timeStr, sizeof(timeStr), "%Y-%m-%d %H:%M:%S", localtime(&now));
    
    fprintf(stderr, "[%s] 错误: %s (函数: %s, 行: %d)\n",
            timeStr, getErrorMessage(code), function, line);
}
```

### 8.2 异常处理策略
- 所有公开接口返回ErrorCode
- 关键操作记录日志
- 数据校验在入口层完成
- 内存分配失败立即终止程序

---

## 九、性能优化策略

1. **哈希表优化**：合理的哈希函数和负载因子
2. **索引优化**：为常用查询建立辅助索引
3. **批量处理**：超时检测采用批量处理减少IO
4. **缓存机制**：热点数据缓存（如自习室信息）
5. **内存池**：频繁分配释放的结构使用内存池

---

## 十、测试用例设计

### 10.1 正常流程测试
1. 学生注册登录
2. 查询可用座位
3. 创建预约
4. 签到签退
5. 预约完成

### 10.2 异常流程测试
1. 重复预约同一时间段
2. 签到超时
3. 空闲超时
4. 信用分不足
5. 座位不可用

### 10.3 边界条件测试
1. 预约开始时间等于当前时间
2. 预约结束时间等于自习室关闭时间
3. 超时阈值刚好达到
4. 内存分配失败处理

---

## 十一、文件结构

```
study_room_system/
├── include/
│   ├── types.h           # 数据类型定义
│   ├── student.h         # 学生管理接口
│   ├── seat.h            # 座位管理接口
│   ├── reservation.h     # 预约管理接口
│   ├── timeout.h         # 超时管理接口
│   ├── statistics.h      # 统计分析接口
│   ├── system.h          # 系统管理接口
│   └── utils.h           # 工具函数接口
├── src/
│   ├── main.c            # 主程序
│   ├── student.c         # 学生管理实现
│   ├── seat.c            # 座位管理实现
│   ├── reservation.c     # 预约管理实现
│   ├── timeout.c         # 超时管理实现
│   ├── statistics.c      # 统计分析实现
│   ├── system.c          # 系统管理实现
│   ├── hashtable.c       # 哈希表实现
│   ├── linkedlist.c      # 链表实现
│   ├── stack.c           # 栈实现
│   ├── queue.c           # 队列实现
│   ├── heap.c            # 堆实现
│   └── utils.c           # 工具函数实现
├── data/
│   ├── students.dat      # 学生数据文件
│   ├── seats.dat         # 座位数据文件
│   └── reservations.dat  # 预约数据文件
├── docs/
│   └── 设计文档.md        # 本文档
├── Makefile              # 编译脚本
└── README.md             # 项目说明
```

---

## 十二、编译与运行

### 12.1 编译命令
```bash
# Linux/Mac
gcc -o study_room_system src/*.c -I include -Wall -Wextra

# Windows (MinGW)
gcc -o study_room_system.exe src/*.c -I include -Wall -Wextra
```

### 12.2 运行命令
```bash
./study_room_system
```

---

## 十三、总结

本系统设计遵循以下原则：
1. **模块化**：功能模块清晰分离
2. **可扩展**：预留扩展接口
3. **高性能**：合理选择数据结构
4. **易维护**：代码结构清晰，注释完善
5. **内存安全**：严格的内存管理

通过合理运用数据结构（链表、哈希表、栈、队列、堆等），实现了高效的预约管理、座位管理和超时检测功能，满足图书馆自习室的日常管理需求。

所有数据结构均使用C语言原生实现，不依赖第三方库，具有良好的可移植性。